{
  "name": "benilike",
  "version": "0.1.0",
  "private": true,
  "description": "Asymmetric corporate roguelike — Benify hackathon 2026",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "install:all": "npm i --prefix client && npm i --prefix server",
    "test:game": "server/node_modules/.bin/ts-node --project scripts/tsconfig.json scripts/test-game.ts",
    "show": "server/node_modules/.bin/ts-node --project scripts/tsconfig.json scripts/e2e-show.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^8.2.2",
    "playwright": "^1.60.0"
  }
}
